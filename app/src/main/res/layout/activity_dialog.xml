<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="diana.seismote.DialogActivity">

    <TextView android:text="dialog"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/textView_dialog"
        android:textSize="23sp" />

    <Button
        android:layout_width="250sp"
        android:layout_height="wrap_content"
        android:text="OK"
        android:id="@+id/button_ok"
        android:onClick="PositiveButtonClicked"
        android:textSize="27sp"
        android:layout_below="@+id/textView_dialog"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true" />

    <Button
        android:layout_width="250sp"
        android:layout_height="wrap_content"
        android:text="CANCEL"
        android:id="@+id/button_cancel"
        android:onClick="NegativeButtonClicked"
        android:textSize="27sp"
        android:layout_below="@+id/textView_dialog"
        android:layout_toLeftOf="@+id/button_ok"
        android:layout_toStartOf="@+id/button_ok" />

</RelativeLayout>
